<% content_for :title, "Add New Company" %>
<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
  <h1 class="h2">Add New Company</h1>
</div>


<%= form_for @company do |f| %>
  
  <div class="form-group row">
    <%= f.label :name %>
    <%= f.text_field :name, autofocus: true, autocomplete: "Company Name", class: 'form-control' %>
  </div>

  <div class="form-group row">
    <%= f.label :address %>
    <%= f.text_field :address, autofocus: true, autocomplete: "Address", class: 'form-control' %>
  </div>

  <div class="form-group row">
    <%= f.label :description %>
    <%= f.text_area :description, autofocus: true, autocomplete: "Description", class: 'form-control' %>
  </div>

  <%= f.submit "Submit", class: 'btn btn-success' %>
  <%= link_to "Cancel", meters_path, class: 'btn btn-secondary' %>

<% end %>